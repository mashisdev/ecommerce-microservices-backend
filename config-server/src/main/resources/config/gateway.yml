server:
  port: 8080

spring:
  application:
    name: gateway
  cloud:
    gateway:
      routes:
        - id: service-example
          uri: http://localhost:8081
          predicates:
            - Path=/api/service-example/**